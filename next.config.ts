import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "https://img.clerk.com",
      "peach-fashionable-sawfish-519.mypinata.cloud",
    ],
  },
};

export default nextConfig;
